language: python
os: linux
addons:
  apt:
    packages:
      - python-numpy
      - python-scipy
      - python-pyside
      - python-configobj
      - python-setuptools
      - avr-libc
      - gcc-avr
matrix:
  include:
    - os: osx
      language: generic
      before_install:
        - pip install pyside
install: pip install pyusb pyqtgraph
script: pushd software && pip install .
